package com.baran.ledger.archfixture;

/**
 * Stands in for a class of ledger-payment-core, which this repository never depends on, so that
 * the INV-9 rule has something to reject. It exists only in the test tree.
 */
public class LedgerType {
}
