package com.baran.recon.archfixture.ledgercode.domain;

import com.baran.ledger.archfixture.LedgerType;

public class Mirror {

    public LedgerType type() {
        return new LedgerType();
    }
}
