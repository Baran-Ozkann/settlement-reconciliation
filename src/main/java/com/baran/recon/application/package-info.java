/**
 * Use cases and the ports they need. Depends on the domain and nothing else (TDD 5.2), and carries
 * no floating point (INV-8). {@code ArchitectureTest} and {@code ci/check-rules.sh} enforce both.
 */
package com.baran.recon.application;
