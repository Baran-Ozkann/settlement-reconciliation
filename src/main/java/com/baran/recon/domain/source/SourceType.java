package com.baran.recon.domain.source;

/** What a configured source delivers, which fixes the format of its files (FR-ING-2, TDD 8.1). */
public enum SourceType {
    PSP_SETTLEMENT,
    BANK_STATEMENT
}
